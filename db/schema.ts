// Database schema (Drizzle ORM).
//
// Tables and pgEnums are added in Phase 1 ("Datový model").
// Kept as an empty module for now so db/client.ts and drizzle-kit
// have something to import during Phase 0.

export {};
